# Handoff：噢易视频、文件链路与本机控制管理

## 接手结论

研究已验证：原广播能显示动态画面；私有 UDP 分片能重组为 H.264；重组结果可被标准解码器解码；转成 MPEG-TS 后可由本机 HTTP 提供给播放器。当前实现是 Python CLI，没有 GUI，也没有 FFmpeg 运行依赖。

研究未完成：**原学生端保持教师连接，同时可靠禁用全部强制键鼠控制。** 不能将临时停止服务、窗口化播放或“当前鼠标能动”视为完成了这个目标。教师当时没有发起控制，最后阶段也没有实时广播，现场联合验收仍缺失。

本仓库提供独立的 `video`、`files` 和 `control` 命令。视频、文件接收不调用本机控制管理，也不会自动启动原学生端。新增的文件接收已经与原厂发送组件在本机互通，真实课堂任务分配仍待验证，详见第五节。

## 一、研究对象与证据

研究对象为 Windows 上的噢易学生端，相关程序版本为 10.9.0.4820。参考过公开项目 [OsEasy-ToolBox](https://github.com/ZiHaoSaMa66/OsEasy-ToolBox/tree/873a074edf4635dadb7ccf01ed3c9e1504bc5773)。其思路提示了原播放器与学生端管理进程可以分别研究；本项目接收器没有复制该项目代码。该项目中的驱动删除、登录入口替换及提权相关做法没有纳入此复现方案。

| 阶段 | 当时的证据 | 能得出的结论 |
| --- | --- | --- |
| 原管理连接 | 原学生端曾显示已连接，并观察到建立的 TCP 连接 | 管理链路存在；不证明视频播放或输入保护 |
| 原生视频基线 | 原播放器显示真实广播，实际观察到课件翻页 | 当时实时视频确实可显示 |
| 独立重组 | 约 6 秒真实样本包含 618 个包，重组出 60 个完整帧 | 所研究私有头部和分片重组方式成立 |
| 像素解码 | 标准解码器对该样本输出 1920×1080 图像，与原播放器内容吻合 | 不是仅收到数据或仅统计到帧 |
| TS / HTTP 联调 | 使用先前真实样本在本机回放，标准解码器成功消费 HTTP 视频 | 封装和输出链路成立；该阶段不属于新的实时课堂测试 |
| Python 移植 | 前序 Python 原型以人工生成的真实 H.264 验证过封装；本仓库保留纯接收/封装核心 | 精简 CLI 的当前测试覆盖结构、HTTP 与入口，不等于现场复测 |
| 文件接收 | 原厂组件在回环网络发送人工目录与文件，Python 接收内容与源逐字节一致 | 内容格式及应答互通；未验证真实课堂任务分配 |

mpv / VLC 使用的是标准 HTTP MPEG-TS 输入方式，但尚未在这次研究中实际运行这两个播放器验证。

## 二、实际复现顺序

### 1. 准备入口和网络参数

准备 uv 和 Python 3.10+，在仓库目录运行：

```powershell
uv sync --locked
uv run oseasy-helper -h
```

用户只需提供教师源 IPv4 和本机课堂网卡 IPv4。组播由 `multicast_group()` 按 `A.B.C.D → 229.1.C.D` 自动推导。这是研究环境观察到、前序 wrapper 已采用的地址约定；不是对所有版本都成立的协议保证。

本机 IPv4 可以只读查询：

```powershell
Get-NetIPAddress -AddressFamily IPv4 | Select-Object InterfaceAlias,IPAddress
```

教师 IP 可从现有学生端设置或已知连接信息取得。若广播自然开始，原播放器的启动参数可提供 `local`、`remote`、`port`：

```powershell
Get-CimInstance Win32_Process -Filter "Name='ScreenRender.exe'" | Select-Object -ExpandProperty CommandLine
```

`local` 对应本机网卡，`remote` 对应实际组播地址，`port` 对应视频目的 UDP 端口。这里的查询用于排查版本差异，正常运行无需用户查找或输入组播。原播放器不在运行时，这条查询可以没有输出。

若已具备抓包环境，可用过滤器观察目标数据：`udp dst port 7778 and src host 203.0.113.10`，替换其中示例教师地址。从 IP 层取得目的组播地址，UDP 层取得负载。CLI 本身不依赖抓包驱动。

### 2. 启动接收器

下面地址仅作示例：

```powershell
uv run oseasy-helper video --teacher 203.0.113.10 --local 192.0.2.20 --udp-port 7778
```

UDP socket 加入指定网卡上自动推导的组播组，过滤教师源 IP，然后进入分片重组。成功启动会输出：

```text
http://127.0.0.1:17778/live.ts
```

整个转换过程在 `oseasy_helper/video.py` 中，可按 `receive → Reassembler.push/drain → State.publish → TsMuxer.mux → handler_for` 阅读。精确字节布局和封装常量见 [PROTOCOL.md](docs/PROTOCOL.md)。

### 3. 打开流并确认画面

```powershell
mpv http://127.0.0.1:17778/live.ts
```

或者将同一地址交给 VLC。播放器从 TS 中读取 PAT/PMT 找到 H.264 PID，从 PES 取得访问单元及时间戳，再进行 H.264 解码、图像显示。Python 只负责传输协议处理与封装，不需要先将视频解码再编码。

在广播实际存在时，验收应逐步确认：已收到完整关键帧提示；播放器出现合理画面；画面随广播变化。只看到 URL、HTTP 200、UDP 包或 H.264 NAL 类型，都不等于最终解码成功。按 Ctrl+C 结束后可查看收包、帧、丢帧及无效包计数。

### 4. 无画面时定位

| 现象 | 优先检查 |
| --- | --- |
| 绑定或加入组播失败 | 本机网卡地址是否存在、端口是否冲突、系统 socket 错误信息 |
| URL 已输出，一直等待关键帧，退出时 packets=0 | 教师是否自然在广播；本机 IP 和教师 IP 是否正确；自动推导的组播是否符合该版本的地址约定；交换机/无线网络是否转发组播；防火墙是否允许接收 |
| packets>0，frames=0 | 私有头部是否仍为所述格式；是否严重缺片；是否已收到同单元 SPS/PPS/IDR |
| frames 增长，播放器不显示 | 播放器是否连到正确 HTTP 端口；码流是否与研究版本兼容；是否需要等下一关键帧 |
| 画面卡顿或连接关闭 | UDP 丢包、播放器缓存或消费过慢；可重新打开地址等待关键帧 |

脚本不自动关闭防火墙、不写入放行规则。现场需要调整时，应限定接收程序、网络接口和 UDP 端口。无需为 HTTP 开放外部入站访问，因为它只监听本机。

## 三、控制链路及本地管理

研究中可见 MMPC 服务、Student、MultiClient、LissHelper 等进程；原管理链路观察过 TCP 9003，也观察过 MultiClient 使用 TCP 9002。视频则走独立的 UDP 组播与 ScreenRender 播放器。这解释了为什么独立接收视频在技术上可以不依赖原播放器窗口。

输入控制还涉及用户态输入模拟/钩子以及已加载的键盘过滤组件。曾观察到 KbFilter；其他组件的精确职责未完成逆向确认。改变 `ScreenRender` 全屏参数只影响窗口行为，不能证明键鼠锁定、输入注入或其他控制路径被关闭。

早期尝试应用沙盒后发生严重内核故障，机器在安全模式恢复。**不再采用沙盒方案。** 本仓库没有沙盒安装器、驱动更改、二进制补丁、输入钩子或自启脚本。

### 临时停止原控制进程，只运行独立视频接收

这是允许原学生端掉线时的模式。步骤均在本机进行：

1. `uv run oseasy-helper control status`：查看原服务和候选进程，记住运行状态。
2. 如需停止，在管理员终端运行 `uv run oseasy-helper control stop`。
3. 再次查询状态；在广播自然存在时运行 `video`，检查画面与本机操作。
4. 需要恢复时运行 `uv run oseasy-helper control start`；原学生端未自动出现则使用原入口启动，核对原客户端连接状态。

`stop` 从 MMPC 注册路径取得安装目录，只处理目录范围内的 Student.exe、MultiClient.exe、LissHelper.exe，避免按同名进程全局结束。它不处理 ScreenRender，不设置 Disabled，不改注册表，不卸载任何驱动。已有原播放器窗口不会由这个命令自动关闭。

如果操作部分完成后失败，应先查询状态，再决定恢复。原服务已经被其他工具设置为 Disabled 时，`start` 可能失败；本 CLI 不擅自恢复未知的旧配置。拒绝访问时也不会追加提权绕过操作。

**停止服务/进程并不能保证已有驱动锁定被解除，也不能阻止其他组件重新启动学生端。** 没有广播和控制同时发生的实际验证，就不能声称“控制已完全关闭且视频一定继续”。

### 保持原学生端在线并阻止全部控制

此目标仍然未实现。缺少各控制入口和驱动交互的完整覆盖，也缺少受控实测。仓库没有提供伪造心跳、显示虚假连接状态或承诺在线隔离的命令。后续研究者应把这项作为独立问题，不能从视频链路成功推导其成立。

## 四、后续验证

- `uv run python -m unittest discover -s tests -v` 运行本机测试，不连接课堂网络，不调用 FFmpeg，也不执行控制启停。
- 后续有自然广播时，用真实参数运行 CLI 并通过播放器确认动态画面；此前不能把精简 CLI 的线上兼容性标记为已验收。
- 不支持音频、丢包重传、未知封装、独立 SPS/PPS 单元或 B 帧 DTS 重建；协议不匹配时应分析最小样本再改实现。

## 五、文件接收链路

文件下发使用独立 TCP 链路，不在视频 H.264 组播中。`feature/file-receive` 分支增加了 `files` 命令：内容传输已在本机与原厂组件互通，真实课堂的任务分配仍待验证。

### 1. 组件、端口与方向

安装目录的 `skin/core.conf` 包含下列默认值；现场配置可能不同。相关组件包括 `FileTransferApp.exe`、`transfer_console.exe`、`client_console.exe` 和 `FileTranferServer.dll`。

| 配置项 | 默认值 | 链路 |
| --- | --- | --- |
| `ConnectPort` | 9003 | 原学生端管理连接，`files` 不建立此连接 |
| `FileNodeManagerPort` | 8555 | 学生文件节点主动连接教师 TCP 端口，收取任务 |
| `FileTransferPort` | 9100 | 学生接收器监听本地 TCP 端口，由发送节点主动连接 |
| `FileReportPort` | 9979 | 文件报告配置，报文尚未完成分析 |

链路顺序：`files → 教师:8555` 建立节点连接；节点连接上收到接收任务；`发送节点 → 本机:9100` 建立数据连接；发送文件头、内容及结束报文；本机在数据连接上应答。

此前现场检查时，原管理端口可连接，文件节点端口超时；短时监听未获得文件任务或数据。**主动连接教师 9100 超时不能证明收件链路故障**：接收方向要求发送节点连接学生本机的监听端口。独立视频收到数据，也不能证明教师已将本机纳入文件任务。

### 2. TCP 帧与节点任务

节点连接和数据连接均使用：`uint32_le payload_length + payload`。长度不包含自己的 4 字节；payload 的前 4 字节是小端 opcode。不能将一次 `recv()` 当成一帧，必须先读满长度，再读满 payload。

文件节点连接后发送 `04 00 00 00 06 00 00 00`，即长度 4、opcode 6；原组件定时器每 20 秒发送相同消息。握手字节经本机运行原组件确认；本 CLI 按该间隔发送，不模拟原学生端的管理身份。

节点消息 opcode 3 表示接收任务；opcode 2 表示发送本机文件。`files` 只解析 3，忽略 2 和其他命令。以下偏移相对于 **payload 中 opcode 后面的任务结构起点**：

| 偏移 | 布局与解释 |
| --- | --- |
| `0x000` | 512 字节首字段；接收实现不使用，完整语义未确认 |
| `0x200` | 80 字节 ASCII、NUL 结尾；接收任务的本地地址 |
| `0x250` | 80 字节 ASCII、NUL 结尾；对应发送节点地址 |
| `0x2a0` | `uint16_le`，文件数据端口 |
| `0x2a2` | 到 `0x2c4` 前的 ASCII 任务标识字段 |
| `0x2c4` | `uint32_le` 子类型；本实现不执行后处理 |
| `0x2c8` | UTF-16 路径字段；本实现不按此路径读写本机文件 |

上述字段从 `client_console.exe` 的任务分发函数（所研究映像 VA `0x423ae0`）及其调用关系确认。本机测试构造了 `0x4d0` 字节的任务结构，但不能据此断言所有任务总长都固定为此值。本实现只要求用于记录的字段完整，并保留有界样本。

原组件接收任务后在 `%APPDATA%\MMC\FileRecv\<任务标识>` 建立暂存目录，再创建监听。Python 版本在启动时就监听 `--local:--data-port`；任务地址及端口只用于日志对照，不触发额外端口、上传或远端路径操作。即使没有先收到节点任务，也能接收明确指定的教师 IP 发来的符合协议的数据连接。

### 3. 文件内容与应答

下表偏移均从**数据 payload 起点**计算；所有整数小端，名字是 UTF-8 相对路径，长度以字节计。每条消息仍须加外层 4 字节长度。

| opcode | payload 布局 | 成功后的应答 |
| --- | --- | --- |
| 0，文件开始 | `u32(0) + u64(file_size) + u32(name_bytes) + name` | 无 |
| 1，普通内容块 | `u32(1) + u32(chunk_bytes) + data` | 无 |
| 2，文件结束 | `u32(2) + u32(tail_bytes) + tail_data`；尾块可以为空 | 裸 `u32(1)` |
| 3，创建目录 | `u32(3) + u32(name_bytes) + name` | 裸 `u32(1)` |
| 4，整次传输结束 | `u32(4)` | 裸 `u32(2)` |

**应答没有长度前缀**。目录创建同样需要应答 1；遗漏会导致原发送端等待。文件结束 opcode 2 可能携带最后一段数据，不能简单忽略其内容。一个数据连接可依次传多个文件、目录，最终由 opcode 4 结束。

静态定位：内容分发函数 VA `0x438a80` 附近，跳转表 `0x4397c0`；目录完成回调 `0x437f60`；应答函数 `0x4377e0`。这些地址仅对应研究映像，不是跨版本稳定接口。应答和文件内容均另外做了运行验证。

Python 对应路径：`receive → receive_connection → read_frame → Transfer.handle`；并行的 `node_loop → node_message` 负责连接任务端口与记录任务。文件写入 `.part`，只有收到文件结束、字节数与声明一致且落盘成功后才改为最终文件名并应答 1。目录成功创建后应答 1，整次结束应答 2。大小不符、未知 opcode、断线或路径不合规则关闭这次连接，保留已有结果，继续监听下一次连接。没有断点续传。

接收目录限定在本次运行目录内，不覆盖已有文件，不执行收到的文件。每次连接有 8 GiB 和 10000 个条目的限制，单帧上限 8 MiB；只接受指定教师地址，暂不接受其他学生节点中继。节点 opcode 2 与数据 opcode 2 是两条不同连接上的不同命令，不能混淆。

### 4. 已完成的本机验证

两方向的兼容性测试全部使用 `127.0.0.1` 和人工文件，测试后结束自建的原组件进程：

1. 本机节点监听 `18555`，以 `127.0.0.1 18555 0 0` 参数启动原 `client_console.exe`，收到 opcode 6。构造上述 opcode 3 任务，把本机及对端地址设为回环，数据端口设为 `19100`。原接收器成功监听。
2. 人工发送目录、文件头、内容分块、带尾块的文件结束和传输结束；分别取得目录应答 1、文件应答 1、传输应答 2，并检查原接收器保存的文件内容一致。
3. 反向在本机建立 Python 数据接收端口；本机节点向原组件发送仅指向人工测试目录的 opcode 2 任务，让原组件在回环网络发送文件。Python 成功接收空目录、中文文件名，以及 1,048,832 字节的二进制测试文件。保存内容与生成源逐字节一致，且完整处理结束应答。

步骤 3 仅用于验证原厂发送格式，**公开 CLI 没有实现发送分支**。自行复做原组件测试时可将子进程 `APPDATA` 指向专门的测试目录，避免暂存文件混入日常目录；所用原组件需要从自己的安装中取得。

仓库内 `tests/test_files.py` 无厂商依赖，可直接运行：

```powershell
uv run python -m unittest discover -s tests -v
```

它包含可复用的人工帧构造方式和本机 TCP 发送示例，覆盖拆分帧、多文件、目录应答、UTF-8 名字、尾块、大小不匹配、截断、路径拒绝、节点握手及上传任务忽略。

### 5. 下次实际下发时验证

```powershell
uv run oseasy-helper files --teacher 203.0.113.10 --local 192.0.2.20
```

使用实际网卡地址和教师地址，保持进程运行直到下发结束。无需额外启动本项目的控制命令。观察 `received/<本次运行目录>/events.jsonl`：

- `node_retry`：文件任务服务连接尚不可用；检查实际任务端口、网络及发送时间。
- `node_connected` 但没有 `receive_task`：目前只能确认节点 TCP 已连通，不能确认已被安排任务。原管理连接或课堂成员状态是否影响分配，仍待现场证据。
- `receive_task` 的 `matches_listener=false`：比较任务端口、本机地址与发送节点地址；本实现不会自动切换到任务里指定的任意端口或中继节点。
- 有匹配任务但没有 `data_connected`：检查发往本机接收端口的数据是否到达、端口占用与入站过滤。单靠教师端口可连接无法排除这里的问题。
- `data_error`：依据报错及样本定位协议差异；保留 `.part`，不要将其当作完整文件。
- `file_saved`：查看实际文件、大小和 SHA-256；`transfer_complete` 说明本地处理了结束报文。教师界面是否显示成功另属完成报告链路。

日志记录时间、主要事件以及节点报文前 4096 字节；错误数据报文保留前 256 字节样本，整个事件文件上限约 16 MiB。它不是全量抓包。下次若失败，保留这份日志即可先定位停在哪一层。

尚未验证或实现：真实课堂下发、学生端离线时的任务分配、不同版本格式、其他节点转发、任务动态端口、并发任务、文件报告端口和原组件的节点完成报告。本地 ACK 及文件落盘成功不等于教师界面已确认完成。
